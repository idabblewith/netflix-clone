
# Mongodbatlas db url (network > add ip address > ensure you select allow access from anywhere)
DATABASE_URL=
# randomly generated
NEXTAUTH_JWT_SECRET=
NEXTAUTH_SECRET=

# github > settings > developer settings > oauth > 
# register app with http://localhost:3000 (OR your prod deployment url) > 
# generate secret and  copy client id
GITHUB_ID=
GITHUB_SECRET=

# console.cloud.google.com > create project > select project > 
# apis and services > oauth consent screen > external > create > 
# fill out required & next > credentials > create oauthclientid >
# fill webap > auth redirect uris: http://localhost:3000/api/auth/callback/google (OR replace localhost:3000 with your deployment url) >
# copy client id and secret
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=